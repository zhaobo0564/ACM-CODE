#include<bits/stdc++.h>
using namespace std;

const int N = 1e5 + 7;
typedef long long ll;

int n, block[N];

ll a[N];

vector<ll> g[N];

int main() {
    ios::sync_with_stdio(0);
    cin >> n;
    int b = sqrt(n);
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
        
    }
}