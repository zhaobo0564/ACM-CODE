#include<bits/stdc++.h>
using namespace std;
const int N = 1000;
typedef long long ll;
ll mp[N][N], n, k;

int main() {
    ios::sync_with_stdio(0);
    cin >> n >> k;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            cin >> mp[i][j];
        }
    }

}