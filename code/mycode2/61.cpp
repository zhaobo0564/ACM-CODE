#include<bits/stdc++.h>
using namespace std;

char s[200], t[200];

int dp[200][200];

int main(){
    cin >> s;
    cin >> t;
    int ls = strlen(s);
    int lt = strlen(t);
    for(int i = 0; i < lt; i++){
        
    }
}
