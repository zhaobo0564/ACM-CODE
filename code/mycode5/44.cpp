#include<bits/stdc++.h>
using namespace std;
const int N = 2e5 + 7;
typedef long long ll;

int t, n;
ll a[N], b[N];
int vis[N];

int main() {
   scanf("%d", &n);
   for (int i = 1; i <= n; i++) {
       scanf("%lld", &a[i]);
        vis[i] = 0;
   }
   for (int i = 1; i <= n; i++) {
       scanf("%lld", &b[i]);
   }
   ll ans = 0;
  

   }
   printf("%lld\n", ans);

}

