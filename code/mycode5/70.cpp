#include<bits/stdc++.h>
using namespace std;

int t, a[200], n;

int main() {
    scanf("%d", &t);
    while (t--) {
        scanf("%d", &n);
        for (int i = n; i; i--) {
            printf("%d ", i);
        }
        puts("");

    }
}